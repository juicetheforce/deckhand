{
  "name": "deckhand",
  "version": "0.1.0",
  "private": true,
  "description": "Personal Stream Deck daemon for Linux",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "npm run build:helper && npm run build:ts",
    "build:ts": "tsc",
    "build:helper": "make -C helper",
    "start": "npm run build:ts && node dist/index.js",
    "decks": "npm run build:ts && node dist/index.js --list",
    "typecheck": "tsc --noEmit",
    "smoke": "npm run build:ts && node scripts/smoke.mjs && node scripts/smoke-socket.mjs && node scripts/smoke-backups.mjs && node scripts/smoke-icons.mjs && node scripts/smoke-labels.mjs && node scripts/smoke-audio.mjs && node scripts/smoke-mpris.mjs && node scripts/smoke-dbus-restart.mjs && node scripts/smoke-defaults.mjs && node scripts/smoke-failures.mjs && node scripts/smoke-latch.mjs && node scripts/smoke-bootstrap.mjs && node scripts/smoke-unattached.mjs && node scripts/smoke-command.mjs && node scripts/smoke-first-run.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@elgato-stream-deck/node": "^7.6.3",
    "dbus-next": "^0.10.2",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "typescript": "^7.0.2"
  },
  "overrides": {
    "dbus-next": {
      "xml2js": "^0.6.2",
      "usocket": "^1.1.0"
    }
  },
  "allowScripts": {
    "@julusian/jpeg-turbo@3.0.1": true,
    "node-hid@3.4.0": true,
    "usocket": false
  }
}
